import React from "react";

import Stack from "react-bootstrap/Stack";

const Vote = ({ template }) => {
  console.log(template);
  return (
    <Stack gap={3}>
      <div>this is to vote</div>
    </Stack>
  );
};

export default Vote;
